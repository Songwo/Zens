server:
  port: 7800
spring:
  # 设置自定义 Banner 文件位置
  banner:
    location: classpath:banner.txt

  # ----------------------------------------------------
  # 2. 数据库配置 (MySQL)
  # ----------------------------------------------------
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/campus_pulse?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
